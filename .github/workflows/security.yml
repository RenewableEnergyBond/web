name: Organization Access Control

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pull-requests: write

jobs:
  external-pr-check:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name != github.repository
    
    steps:
    - name: Comment on external PR
      uses: actions/github-script@v7
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: '👋 Merci pour votre contribution ! Cette PR provient d\'un fork externe et nécessite une review manuelle par notre équipe avant d\'être mergée.'
          });